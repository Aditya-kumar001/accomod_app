import 'package:get/get.dart';

class SearchRoomListController extends GetxController {

  @override
  void onInit() {
    super.onInit();
  }

  void showPreferenceValue() {
    // This method can be used to toggle the visibility of preferences
    // or to perform any action related to showing preferences.
    // Currently, it does nothing but can be implemented as needed.
  }

  void sortList() {
    // This method can be used to handle sorting logic.
    // Currently, it does nothing but can be implemented as needed.
  }

  void fliterList() {
    // This method can be used to handle filtering logic.
    // Currently, it does nothing but can be implemented as needed.
  }

  void mapsList() {
    // This method can be used to handle map-related logic.
    // Currently, it does nothing but can be implemented as needed.
  }

  @override
  void onClose() {
    super.onClose();
  }

}
